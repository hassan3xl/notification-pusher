[project]
name = "notify-python"
version = "0.1.0"
description = "Python client library for the Notification Server"
dependencies = [
    "requests>=2.28.0"
]
requires-python = ">=3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
